apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: voting-app-postgres
  namespace: voting-app
spec:
  project: default
  source:
    repoURL: https://github.com/AgustinLandriel/gitops-voting-app.git
    targetRevision: HEAD
    path: k8s/postgres
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
